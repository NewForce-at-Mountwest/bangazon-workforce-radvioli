@model IEnumerable<BangazonWorkforce.Models.ViewModels.DepartmentEmployeeListViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.ThisDepartment.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThisDepartment.budget)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepartmentSize)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.ThisDepartment.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThisDepartment.budget)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartmentSize)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { @*id=item.id*@}) |
                @Html.ActionLink("Details", "Details", new { id = item.ThisDepartment.id})
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
